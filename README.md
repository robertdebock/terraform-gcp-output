# Terraform GCP Output

A repository to try `output`.

## Setup

```shell
test -f id_rsa || ssh-keygen -f id_rsa
terraform init
```

## Running

```shell
terraform plan
terraform apply
terraform output
terraform destroy
```
